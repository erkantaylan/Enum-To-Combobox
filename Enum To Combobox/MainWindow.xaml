<Window x:Class="Enum_To_Combobox.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Enum_To_Combobox"
        mc:Ignorable="d"
        Title="MainWindow"
        Height="450"
        Width="800"
        d:DataContext="{d:DesignInstance local:ViewModel}">
    <Window.Resources>
        <local:ImageFormatValueConverter x:Key="ImageFormatValueConverter" />
    </Window.Resources>
    <Grid>
        <ComboBox ItemsSource="{Binding Source={StaticResource ImageFormatValueConverter}, Path=Strings}"
                  SelectedItem="{Binding Format, Converter={StaticResource ImageFormatValueConverter}}"
                  VerticalAlignment="Top"
                  Padding="10"
                  Margin="10" />
    </Grid>
</Window>